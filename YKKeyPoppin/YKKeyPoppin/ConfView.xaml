<YK:Window x:Class="YKKeyPoppin.ConfView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:YK="clr-namespace:YKToolkit.Controls;assembly=YKToolkit.Controls"
           Title="ConfView" Height="300" Width="300"
           ShowInTaskbar="False"
           IsClosingConfirmationEnabled="False"
           Topmost="True"
           MinimizeButtonVisibility="Collapsed"
           RestoreButtonVisibility="Collapsed"
           MaximizeButtonVisibility="Collapsed"
           TopmostButtonVisibility="Collapsed"
           >
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="0,10,10,10" Content="OK" Click="ClickOkButton" />
            <Button Width="80" Margin="0,10,10,10" Content="Cancel" Click="ClickCancelButton" />
        </StackPanel>

        <Grid>
            <TextBlock x:Name="textblock" />
        </Grid>
    </DockPanel>
</YK:Window>
