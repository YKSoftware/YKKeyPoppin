<UserControl x:Class="YKKeyPoppin.Views.KeyConfView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Conf Keys :" Margin="4" />
        <StackPanel>
            <TextBlock Text="Press any key to register input text automatically." />
            <TextBlock Text="{Binding InputKey, StringFormat='{}Key = {0}'}" />
            <TextBlock Text="{Binding InputText, StringFormat='{}String = {0}'}" />
        </StackPanel>
    </DockPanel>
</UserControl>
