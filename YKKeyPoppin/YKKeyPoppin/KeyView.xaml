<Window x:Class="YKKeyPoppin.KeyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YKKeyPoppin"
        local:SystemMenuBehavior.IsEnabled="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        >
    <Grid x:Name="grid">
        <TextBlock x:Name="textblock" FontSize="44" Foreground="#FF27CED7" FontFamily="Meiryo UI" Margin="0,0,4,4">
            <TextBlock.Effect>
                <DropShadowEffect BlurRadius="4" Color="Black" ShadowDepth="4" />
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
