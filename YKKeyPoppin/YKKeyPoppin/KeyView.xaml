<Window x:Class="YKKeyPoppin.KeyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YKKeyPoppin"
        local:SystemMenuBehavior.IsEnabled="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        Topmost="True"
        Opacity="0"
        >
    <TextBlock x:Name="textblock" FontSize="44" Foreground="White" FontFamily="Meiryo UI">
        <TextBlock.Effect>
            <DropShadowEffect BlurRadius="4" Color="Black" ShadowDepth="4" />
        </TextBlock.Effect>
    </TextBlock>
</Window>
